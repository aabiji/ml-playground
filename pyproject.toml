[project]
name = "ml-playground"
version = "0.1.0"
description = ""
readme = "README.md"
authors = [
    { name = "Abigail Adegbiji", email = "adegbijiabigail@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "torch",
    "torchvision",
    "numpy",
    "matplotlib",
    "datasets",
    "datasets[vision]",
    "requests",
    "jupyter>=1.1.1",
    "ipykernel>=7.3.0",
]

[tool.uv]
package = false

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
